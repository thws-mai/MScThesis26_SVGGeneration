\chapter{Vector Representation in SVG Generation}
\label{ch:representation}

SVG generation models differ not only in their architectures and 
training objectives but also in how they represent vector graphics 
internally during the generation process. While all approaches 
ultimately produce SVG files as output, the internal representation 
used during learning or optimization varies considerably across 
methods. This variation has direct consequences for how geometry is 
parameterized, how supervision signals are applied, and what 
structural properties the resulting graphics have.

In the context of this thesis, representation refers to the 
intermediate form in which a model encodes vector geometry, not 
the final output file but the internal structure through which 
shapes are created, manipulated, and decoded. As introduced in 
Chapter~\ref{ch:Background}, a vector graphic can be represented 
in several fundamentally different ways: as a collection of 
explicit geometric primitives with directly optimizable parameters, 
as an ordered sequence of drawing commands, as the weights of a 
neural network that maps indices or coordinates to shape properties, 
or as a compact latent embedding from which structured output is 
decoded. Each of these formulations encodes the same underlying 
geometry differently, and this difference shapes how the model 
learns, how gradients propagate, and what the generated SVG looks 
like structurally.

This chapter examines four primary representation families 
identified across the selected models: parametric, sequential, 
implicit neural, and latent. For each family, the analysis 
describes how it works technically, which models use it, and what 
structural tendencies it introduces. 
Figure~\ref{fig:representation_outputs} shows qualitative outputs 
from representative models across the four families, illustrating 
how these representational choices translate into visible structural 
differences.


\section{Parametric Representations}

Parametric representations describe vector graphics as collections 
of explicit geometric primitives, specifically paths defined by 
B\'{e}zier control points, stroke attributes, and fill colors. 
These parameters can be optimized directly using gradient-based 
methods. The representation closely matches the SVG format itself 
(Section~\ref{sec:vector_fundamentals}), which is why it feels 
like the most natural starting point for SVG generation.

Once DiffVG made rasterization 
differentiable~\cite{liDifferentiableVectorGraphics2020a} 
(Section~\ref{sec:diffvg}), it became possible to optimize these 
parameters directly against image-based or semantic objectives. 
LIVE, SAMVG, VectorFusion, and SVGDreamer are all built on this 
foundation~\cite{maLayerwiseImageVectorization2022, 
zhuSAMVGMultistageImage2023, 
jainVectorFusionTexttoSVGAbstracting2022, 
xingSVGDreamerTextGuided2024}.

In practice, these models initialize a fixed set of vector 
primitives and iteratively refine their parameters so that the 
rendered image aligns with a target signal. At each step, the 
model holds a list of paths, each described by a handful of 
numbers. It renders them, computes the loss, and nudges those 
numbers slightly. That is the entire loop. The initialization 
strategy varies: LIVE uses a layer-by-layer approach that 
progressively adds paths, while SVGDreamer's SIVE module uses 
diffusion model attention maps to determine where primitives 
should be placed before optimization 
begins~\cite{xingSVGDreamerTextGuided2024}. Despite these 
differences, all of these methods share the same underlying 
commitment: shapes are stored as explicit geometric parameters 
that are modified directly during optimization.

The predictable failure mode is that complexity is handled by 
adding more paths. When a model needs to capture fine visual 
detail, it introduces overlapping or redundant primitives rather 
than increasing the expressiveness of individual ones. LIVE uses 
160 paths across five layers to represent a single 
image~\cite{maLayerwiseImageVectorization2022}. At this scale, 
the output is theoretically editable since each path is a 
geometric object, but in practice the number and entanglement of 
paths makes meaningful manual editing very difficult. 
SVGDreamer++ explicitly identifies path entanglement as a failure 
mode of optimization-based parametric 
methods~\cite{xingSVGDreamerAdvancingEditability2025}. Open a 
LIVE output in Illustrator and you will see what this means: the 
image looks fine, but trying to select a single object means 
clicking through dozens of overlapping fragments.


\section{Sequential Representations}

Sequential models produce the most compact and organized outputs in 
the entire analyzed set, at least within their training domain. 
Paths are few, shapes are distinct, and the resulting SVG files 
are small enough to read by hand. The reason is that sequential 
representations treat vector graphics as ordered sequences of 
drawing commands, mirroring the actual structure of SVG path data.

The idea of modeling vector graphics this way goes back to 
SketchRNN~\cite{haNeuralRepresentationSketch2017}, which showed 
that recurrent networks could learn stroke dynamics from data. 
DeepSVG extended this to structured SVG icons using a hierarchical 
VAE with a transformer 
decoder~\cite{carlierDeepSVGHierarchicalGenerative2020}. 
DeepIcon~\cite{bingDeepIconHierarchicalNetwork2024} follows the 
same sequential formulation for icon generation. Rather than 
storing shapes as independent geometric objects, these models 
generate a series of commands that describe how the graphic is 
constructed step by step.

The catch is that the representation puts a practical ceiling on 
complexity. Every additional command in the sequence increases the 
chance that prediction errors accumulate and break what comes after. 
This is why sequential models work well for icons (short sequences, 
simple structure) but struggle with complex scenes. DeepSVG trains 
on SVG-Icons8~\cite{carlierDeepSVGHierarchicalGenerative2020}, 
which consists of simple icons with small path counts, and the 
outputs reflect that constraint. There is also a more fundamental 
limitation: a procedural command sequence does not encode explicit 
spatial hierarchy. The model generates commands in order but nothing 
in the representation directly encodes which commands belong to 
which semantic region. Editability is therefore limited even when 
visual reconstruction quality is high.



\section{Implicit Neural Representations}

NeuralSVG represents each SVG as a set of shape indices, where 
each index corresponds to a single shape, and a small MLP learns 
to map these indices to B\'{e}zier control points and fill 
colors~\cite{polaczekNeuralSVGImplicitRepresentation2025}. The 
network weights are the representation, not the shapes themselves. 
This is a fundamentally different approach from storing control 
points or commands explicitly: the geometry only exists when you 
query the network for it.

The idea builds on the same principle as 
NeRF~\cite{mildenhallNeRFRepresentingScenes2020}, where a neural 
network defines geometry implicitly through learned weights rather 
than through stored coordinates. NeuralSVG maintains stable shape 
correspondence during training, meaning each index always maps to 
the same visual region, which prevents the network from producing 
inconsistent outputs across iterations. NiVeL follows a related 
approach, encoding the entire layered structure implicitly where 
each layer is defined by a continuous neural function over the 
image 
domain~\cite{thamizharasanNIVeLNeuralImplicit2024}, enabling 
clean layered decompositions without explicit path initialization.

The outputs from both models tend to look globally coherent. 
Layers are clean, shapes do not bleed into each other, and the 
overall composition holds together in a way that purely parametric 
outputs often do not. The cost is that individual shapes are not 
directly accessible until the network output is converted into 
discrete primitives, and how clean that conversion is determines 
whether the final SVG supports practical editing.

\section{Latent Representations}

There are no control points, no path commands, no shape boundaries 
stored in a latent vector. What the latent space does is compress 
many graphics into a shared numerical embedding learned during 
training, and generation occurs by decoding these embeddings into 
one of the other representation types (parametric paths or 
sequential commands) underneath. This distinction was introduced in 
Section~\ref{sec:latent_repr}: a latent space is not a geometric 
representation but an organizational layer that sits on top of one. 
Several models use latent spaces this way.

DeepSVG applies this by encoding path command sequences into latent 
vectors and training a transformer decoder to reconstruct 
them~\cite{carlierDeepSVGHierarchicalGenerative2020}, building on 
the VAE framework~\ref{sec:latent_repr}. The latent space captures 
structural regularities across the training dataset, enabling 
sampling of new SVGs by decoding from this space.

SVGFusion~\cite{xingSVGFusionScalableTexttoSVG2025} extends this 
further by running a diffusion process entirely within the learned 
latent space of a pretrained vector encoder, rather than in pixel 
space. T2V-NPR takes a different approach, arguing that global 
SVG-level latent spaces are too constrained to capture diverse 
path combinations, and instead learns a neural path representation 
at the individual path 
level~\cite{zhangTexttoVectorGenerationNeural2024}. Generation is 
fast and the outputs feel stylistically consistent, but adjusting 
one specific shape requires navigating latent space rather than 
clicking a path, which is not how designers work.


\section{Hybrid Representations}
\label{sec:hybrid}

In practice, many models combine elements from more than one 
representation family, because no single representation fully 
addresses the competing requirements of structural correctness, 
editability, scalability, and visual realism.

VectorFusion and SVGDreamer use parametric primitives as the 
direct optimization target while relying on a latent diffusion 
model as the semantic prior. The parametric layer provides 
geometric interpretability, while the latent model provides 
semantic coherence that pure geometric optimization cannot achieve 
alone. SVGFusion takes a similar combined approach, encoding 
path-level structure into latent embeddings before applying 
diffusion.

LayerTracer~\cite{songLayerTracerCognitiveAlignedLayered2025} 
represents a different kind of hybrid. The model is trained on 
the Serpentine dataset, which contains step-by-step human SVG 
construction sequences, meaning the training data encodes a 
sequential procedural representation. The output the model learns 
to produce, however, is a layered parametric SVG where each layer 
corresponds to a meaningful visual component. Editability becomes 
a learned property rather than an emergent one, because the model 
was trained on how humans draw, it produces outputs structured 
the way a human designer would structure them, with clean layer 
separation and manageable path counts.

NeuralSVG also combines ideas across families. Its core 
representation is implicit, with an MLP mapping shape indices to 
geometric parameters, but it incorporates an ordered assignment 
mechanism originally introduced in DeepSVG to ensure shape 
indices correspond consistently to visual regions across 
training~\cite{polaczekNeuralSVGImplicitRepresentation2025}. 
This ordered structure imposes explicit hierarchy on what is 
otherwise a purely neural representation.

These hybrid strategies show that representation design in SVG 
generation is less about choosing one family and more about 
deciding which trade-offs to accept.


\begin{figure}[ht]
    \centering
    \includegraphics[width=0.50\textwidth]{representation.png}
    \caption{Outputs from four models across the four representation 
    families, all depicting a rocket. Top left: DeepSVG 
    (sequential), from the SVG-Icons8 
    dataset~\cite{carlierDeepSVGHierarchicalGenerative2020}. Top 
    right: SVGDreamer (parametric). Bottom left: SVGFusion 
    (latent). Bottom right: NeuralSVG (implicit neural). The 
    SVGDreamer and SVGFusion outputs are reproduced 
    from~\cite{xingSVGFusionScalableTexttoSVG2025}, the NeuralSVG 
    output 
    from~\cite{polaczekNeuralSVGImplicitRepresentation2025}.}
    \label{fig:representation_outputs}
\end{figure}


\section{Representation, Editability, and Structural Outcomes}
\label{sec:repr_editability}

Open any SVG in Illustrator or Figma and you expect to click on a 
shape and move it. SVGs are built from individually selectable 
elements, that is the whole point. Whether an AI-generated SVG 
actually supports that depends more on how the model represents 
geometry internally than on how the rendered output looks.

The four rockets in Figure~\ref{fig:representation_outputs} show 
what this means in practice. DeepSVG produces the rocket as a 
monochrome icon with around 15 to 30 paths. The reason it looks 
like an icon and not a photorealistic scene is tied directly to 
the sequential representation: longer command sequences mean more 
accumulated error, so the training data and therefore the outputs 
stay simple. A designer could open that file and rearrange things. 
SVGDreamer generates the same subject using somewhere between 256 
and 512 optimized B\'{e}zier 
paths~\cite{xingSVGDreamerTextGuided2024}. The result is visually 
rich but structurally opaque, because most of those paths overlap 
and do not correspond to any recognizable component. NeuralSVG 
renders its rocket with exactly 16 shapes, each mapped through an 
MLP~\cite{polaczekNeuralSVGImplicitRepresentation2025}, a roughly 
30x reduction in element count for the same subject. And SVGFusion 
sits in between, producing roughly 10 to 20 structured shapes 
including not just B\'{e}zier paths but also basic SVG elements 
like circles and 
rectangles~\cite{xingSVGFusionScalableTexttoSVG2025}, because its 
latent space was trained on real SVG files that use the full 
primitive vocabulary.

\begin{table}[ht]
\centering
\caption{Structural consequences of representation choice across 
the four families. Path counts reflect default configurations 
reported in the respective papers.}
\label{tab:repr_consequences}
\begin{tabular}{l c l l}
\hline
\textbf{Representation} & \textbf{Path Counts} & 
\textbf{Editability} & \textbf{Typical Output} \\
\hline
Parametric & 160--512 & Low & Visually rich, paths, but overlap heavily \\
\hline
Sequential & 15--30 & Medium & Clean icons, but breaks on complex scenes \\
\hline
Implicit neural & 16 (fixed) & High & Few shapes, each one has semantic meaning\\
\hline
Latent & 10--20 & Medium & Uses full SVG vocab, looks designed \\
\hline
\end{tabular}
\end{table}

What this comparison reveals is that the number of shapes a model 
produces, and whether those shapes correspond to anything 
meaningful, matters more for practical editability than which 
representation family the model belongs to. Parametric 
representations should in theory be the most editable since every 
shape is an explicit geometric object. But that advantage vanishes 
when the path count gets into the hundreds. Sequential and latent 
representations produce compact output, but within bounded domains. 
Implicit representations give the cleanest decomposition, but at 
a computational cost that limits practical use.

What actually seems to drive clean structure is not the 
representation itself but what constraints are placed on it. 
Methods that use segmentation masks to initialize primitives 
(SAMVG)~\cite{zhuSAMVGMultistageImage2023}, saliency maps to place shapes (NeuralSVG)~\cite{polaczekNeuralSVGImplicitRepresentation2025}, or 
construction-sequence training data (LayerTracer)~\cite{songLayerTracerCognitiveAlignedLayered2025} all produce 
outputs with clearer boundaries regardless of their underlying 
representation type. LayerTracer is the clearest example: 
editability there is something the model was explicitly taught. 
Representation defines what kinds of structures are possible, but 
initialization and supervision determine whether useful structure 
actually shows up in the output.


\section{Summary}

These observations provide the foundation for the next analytical 
dimension of the thesis. While representation defines how vector 
graphics are encoded, supervision determines how models learn to 
shape that representation. The following chapter examines 
supervision signals and guidance strategies, analyzing how they 
interact with representation choices to influence the quality, 
structure, and behavior of generated SVG graphics.